# ═══════════════════════════════════════════════════
# Scalara Webmail — Environment Variables
# ═══════════════════════════════════════════════════
#
# Architecture:
#   VPS  → Runs email engine (Postfix/Dovecot) + PostgreSQL
#   Here → Webmail (Next.js) hosted on Vercel or any Node.js host
#
# After running the installer on your VPS, copy the values
# from /opt/scalara/config/webmail.env on your VPS.
# ═══════════════════════════════════════════════════

# ── Database (connects to VPS PostgreSQL remotely) ──
DATABASE_URL="postgresql://scalara:YOUR_DB_PASSWORD@YOUR_VPS_IP:5432/scalara?sslmode=require"

# ── Authentication ──
NEXTAUTH_URL="https://your-webmail-domain.com"
NEXTAUTH_SECRET="your-nextauth-secret"

# ── Mail Server (connects to VPS IMAP/SMTP remotely) ──
IMAP_HOST="mail.yourdomain.com"
IMAP_PORT=993
SMTP_HOST="mail.yourdomain.com"
SMTP_PORT=587

# ── Encryption (MUST match the VPS engine) ──
ENCRYPTION_KEY="your-64-char-hex-encryption-key"

# ── App ──
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
